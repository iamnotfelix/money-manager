import { Stack, Typography } from "@mui/material";

export const UserDetailed = () => {
    return (
        <Stack direction="column">
            <Typography variant="h6">Detailed user</Typography>
        </Stack>

    );
}