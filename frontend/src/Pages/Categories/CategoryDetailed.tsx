import { Stack, Typography } from "@mui/material";

export const CategoryDetailed = () => {
    return (
        <Stack direction="column">
            <Typography variant="h6">Detailed Category</Typography>
        </Stack>

    );
}