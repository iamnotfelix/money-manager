import { Stack, Typography } from "@mui/material";

export const Loading = () => {
    return (
        <Stack alignItems="center" mt={4}><Typography variant="h3" gutterBottom>Loading...</Typography></Stack>
    );
}